# 第一周 任务清单与验收标准

---

## 侯增豪 — IoT 基础设施

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | 树莓派：烧录 OS，SSH 可达，安装 Docker，固定内网 IP | 1d | **P0** | 吕志远 Git 仓库就绪（代码版本管理）；吕志远 看板就绪（任务跟踪） | 无 |
| 2 | Mosquitto：Docker 运行 eclipse-mosquitto:2，端口 1883，用户名密码认证，ACL 放行 aiguard/v1/#，持久化 | 0.5d | **P0** | 树莓派就绪（须在树莓派上部署） | 曾荣基（手表连 Mosquitto 联调）、江松涛（后端 MQTT 联调） |

## 吕志远 — SM（Scrum Master）

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | Gitee 仓库：main+dev 分支，README，邀 9 人，main 保护 | 0.5d | **P0** | 无 | **全体**（需入仓 clone） |
| 2 | 看板搭建：飞书/TAPD，Backlog/Todo/Doing/Test/Done 列 | 0.5d | **P0** | 无 | **全体**（需录入各自任务） |
| 3 | 每日站会主持(15min)、看板维护、阻塞跟踪 | 全程 | **P0** | 看板就绪 | **全体**（每日汇报） |
| 4 | Sprint 评审组织 + 会议纪要 | 1d | **P1** | 各模块开发基本完成（周四前） | **全体**（演示各自模块） |

## 仇伟烨 — ESP32-P4 大屏

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | ESP-IDF v5.5.2 + LVGL 9.x，7 寸屏驱动 RGB565/8080，触摸初始化 | 1d | P1 | 吕志远 Git 仓库就绪 | 曾荣基（共用 IDF 环境经验） |
| 2 | WiFi + MQTT：连 Mosquitto，订阅 aiguard/v1/+/alert/sos | 0.5d | P1 | 侯增豪 Mosquitto 就绪 | 曾荣基（MQTT 联调） |
| 3 | SOS 弹窗：全屏红色(类型+老人名+时间)，200ms 顶滑入，点击/5min 关闭，多次叠加最新置顶 | 1.5d | P1 | 大屏 WiFi+MQTT 就绪；江松涛确认 SOS 消息 JSON 格式 | 江松涛（消息格式对齐） |
| 4 | 片区概览雏形：标题栏+NTP 时间，2x2 老人卡片(Mock) | 1d | P1 | LVGL 就绪 | 无 |

## 杨深铭 — 后端核心 API

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | Spring Boot 3.3.6+Maven 骨架：web/mybatis-plus/security/jwt/lombok/postgresql | 0.5d | **P0** | 吕志远 Git 仓库就绪 | 江松涛（共用同一项目） |
| 2 | 数据库建表：community/user/elderly/device/alert/dispatch 6 表，含主键+索引+测试数据(2社区+5老人+3设备+3用户) | 0.5d | **P0** | 骨架就绪 | 江松涛（确认表结构） |
| 3 | 数据源配置：datasource + mybatis-plus + jackson(ISO 8601) + SQL 日志 | 0.5d | **P0** | 骨架就绪、数据库就绪 | 无 |
| 4 | JWT 认证：POST /api/v1/auth/login，BCrypt，Token 24h，返回 {code:0,data:{token,userId,role,realName},ts} | 0.5d | **P0** | 数据库就绪（user 表有测试数据） | **李凯辉**（登录页联调）、**徐齐晖**（后台登录联调）、**邹双声**（App 登录联调） |
| 5 | 角色权限：ADMIN/WORKER/FAMILY 三级拦截 | 0.5d | **P0** | JWT 就绪 | 无 |
| 6 | Community API：GET/POST/PUT/DELETE，删除时有老人/用户则拒绝 | 0.5d | **P0** | 数据库就绪 | 徐齐晖（社区管理页联调） |
| 7 | User API：GET/POST/PUT/DELETE，密码 BCrypt，分页 | 0.5d | **P0** | 数据库就绪 | 徐齐晖（用户管理页联调） |
| 8 | Elderly CRUD：GET/POST/PUT/DELETE，communityId 不存在->400，删除级联解绑设备 | 1d | **P0** | 数据库就绪 | 徐齐晖（老人管理页联调）、李凯辉（老人列表联调） |
| 9 | Device CRUD：GET/POST/PUT/DELETE，POST /{id}/bind + /{id}/unbind | 0.5d | **P0** | Elderly API 就绪（绑定需要 elderlyId） | 徐齐晖（设备管理页联调）、曾荣基（设备绑定确认） |

## 江松涛 — MQTT 消费 + 业务 API

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | MQTT 消费：Spring Integration 监听 SOS QoS2 + 心跳 QoS1，topic 分流，断线自动重连 | 1d | **P0** | 侯增豪 Mosquitto 就绪（连接地址+端口）；杨深铭 数据库就绪（查 device 表） | 侯增豪（Broker 连接验证） |
| 2 | SOS 入库：解析 JSON -> 查 device -> Alert(PENDING,SOS,HIGH) | 0.5d | **P0** | MQTT 消费就绪 | 曾荣基（对齐 SOS 消息 Payload 格式） |
| 3 | 心跳处理：更新 device.last_heartbeat + status=ONLINE + battery | 0.5d | **P0** | MQTT 消费就绪 | 曾荣基（对齐心跳消息格式） |
| 4 | Alert API：GET 列表(communityId/status/type/page/size)+GET /{id} 详情+PATCH /{id} 状态单向流转 | 1d | **P0** | SOS 入库就绪（alert 表有数据才能测） | 李凯辉（告警列表联调） |
| 5 | Dispatch API：POST(创建+alert->PROCESSING)+GET+PATCH(处理结果+双表同步) | 1d | **P0** | Alert API 就绪 | 李凯辉（接单处理联调） |
| 6 | Stats overview：GET /api/v1/stats/overview 返回 4 字段(totalElderly,todayAlerts,onlineDevices,pendingAlerts) | 0.5d | P1 | 各表有数据 | 徐齐晖（Dashboard 联调） |

## 李凯辉 — 社区工作台

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | Vue3+Vite+TS 骨架，Element Plus+Axios，npm run dev 启动 | 0.5d | **P0** | 吕志远 Git 仓库就绪 | 无 |
| 2 | Axios 封装：baseURL /api/v1，自动注入 Token，响应拦截(非0->message，401->跳登录) | 0.5d | **P0** | 骨架就绪 | 徐齐晖（可复用相同封装） |
| 3 | 路由：/login / /alerts，beforeEach 守卫，404 页面 | 0.5d | **P0** | 骨架就绪 | 无 |
| 4 | 登录页：表单验证(非空+密码>=6位)+loading 防重复提交，对接 POST /api/v1/auth/login | 0.5d | **P0** | 杨深铭 JWT API 就绪 | 杨深铭（联调登录接口） |
| 5 | 工作台首页：顶栏+待处理告警区(最新5条PENDING红色卡片)+快捷入口 | 0.5d | **P0** | 登录页就绪 | 无 |
| 6 | 告警列表：Tab 切换 PENDING/PROCESSING/RESOLVED/全部，类型图标+老人名+类型中文+时间+状态标签，分页 | 1d | **P0** | 江松涛 Alert API 就绪 | 江松涛（联调列表接口） |
| 7 | 告警详情：类型+老人信息+时间线，PENDING->接单按钮，PROCESSING->处理弹窗，RESOLVED->结果 | 0.5d | **P0** | 告警列表就绪、Dispatch API 就绪 | 江松涛（联调详情接口） |
| 8 | 接单+处理：接单确认弹窗->POST /api/v1/dispatches；处理弹窗(结果下拉+描述)->PATCH | 1d | **P0** | 告警详情就绪、江松涛 Dispatch API 就绪 | 江松涛（联调派单接口）、徐齐晖（并发接单测试） |

## 徐齐晖 — 管理后台

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | Vue3 管理后台骨架：AdminLayout(可折叠菜单+顶栏+内容区)，菜单：数据总览/社区/老人/用户/设备/告警 | 0.5d | **P0** | 吕志远 Git 仓库就绪 | 李凯辉（可复用 Vue3+Axios 配置） |
| 2 | 登录页(同 F1)，登录后按 role 跳转：ADMIN->Dashboard，WORKER->社区工作台 | 0.5d | **P0** | 杨深铭 JWT API 就绪 | 杨深铭（联调登录） |
| 3 | 数据总览：4 卡片(老人总数/今日告警/在线设备/待处理) | 0.5d | P1 | 江松涛 Stats overview API 就绪 | 江松涛（联调统计接口） |
| 4 | 社区管理：表格(ID/名称/地址/区域/老人数/操作)，新增/编辑弹窗，删除(有老人->禁止) | 0.5d | **P0** | 杨深铭 Community API 就绪 | 杨深铭（联调 CRUD） |
| 5 | 老人管理：表格(ID/姓名/年龄/性别/社区/状态/设备/操作)，新增/编辑弹窗(含社区下拉)，搜索 | 0.5d | **P0** | 杨深铭 Elderly API 就绪 | 杨深铭（联调 CRUD） |
| 6 | 用户管理：表格(ID/用户名/姓名/角色标签/社区/操作)，新增弹窗(角色+社区下拉)，编辑密码不填则不变 | 1d | **P0** | 杨深铭 User API 就绪 | 杨深铭（联调 CRUD） |
| 7 | 设备管理：表格(ID/名称/类型/MAC/社区/绑定老人/状态/心跳)，绑定弹窗(仅本社区未绑老人) | 0.5d | **P0** | 杨深铭 Device API 就绪 | 杨深铭（联调绑定） |
| 8 | 告警管理：表格(ID/类型+老人名/社区/状态标签/时间/操作)，筛选栏：状态+类型+社区 | 0.5d | P1 | 江松涛 Alert API 就绪 | 江松涛（联调筛选） |

## 曾荣基 — PO + ESP32-S3 手表

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | PO：Backlog 管理、验收标准维护、演示脚本编写 | 全程 | **P0** | 无 | **全体**（确认各自验收标准） |
| 2 | ESP-IDF v5.5.2 环境安装，hello_world 烧录 | 0.5d | **P0** | 吕志远 Git 仓库就绪 | 仇伟烨（共用 IDF 经验） |
| 3 | WiFi 联网：menuconfig 配 SSID/Password，串口显示 IP | 0.5d | **P0** | IDF 环境就绪 | 无 |
| 4 | MQTT 通信：ESP-MQTT 连 Mosquitto，订阅 aiguard/v1/watch/{mac}/cmd/#，发布测试消息 | 0.5d | **P0** | 侯增豪 Mosquitto 就绪, WiFi 就绪 | 侯增豪（Broker 联调）、江松涛（后端消费验证） |
| 5 | SOS 按键：GPIO 中断，防抖 500ms，长按 3s，QoS 2 发布，Payload 含 type/deviceId/timestamp/data(elderlyId,lat,lng)，OLED 显示 SOS 已发送 | 2d | **P0** | MQTT 通信就绪 | 江松涛（对齐 SOS Payload 格式）、侯增豪（MQTT QoS 验证） |
| 6 | 心跳上报：60s 周期 QoS 1，含 battery+rssi | 0.5d | **P0** | SOS 按键就绪（代码结构复用） | 江松涛（后端心跳处理联调） |
| 7 | OLED 显示：时间(NTP)+电量+MQTT 连接状态+SOS 提示 | 0.5d | **P0** | IDF 环境就绪、WiFi 就绪（NTP 需要网络） | 无 |

## 邹双声 — 家人端 App

| # | 任务 | 估时 | 优先级 | 先做条件 | 协作人 |
|:-|------|:---:|:------:|:---------|:-------|
| 1 | DevEco Studio 环境搭建，Hello World 跑模拟器 | 0.5d | **P0** | 吕志远 Git 仓库就绪 | 无 |
| 2 | ApiService 封装：baseURL 配置，自动注入 Token，统一 JSON 解析+错误处理 | 0.5d | **P0** | 环境就绪 | 杨深铭（确认 API 返回格式） |
| 3 | 登录页：role 检查(FAMILY/ADMIN->家人首页，ELDERLY->拒绝) | 0.5d | **P0** | 杨深铭 JWT API 就绪 | 杨深铭（联调登录） |
| 4 | 家人端首页：顶栏+关联老人名，老人卡片(姓名/年龄/状态灯/最近活跃)，未读告警红点+最近3条摘要 | 1.5d | **P0** | 登录页就绪、Alert API 就绪（未读数） | 江松涛（联调告警数据） |
| 5 | 家人端告警列表+老人档案：GET /api/v1/alerts?elderlyId=，Tab 筛选；GET /api/v1/elderly/{id} 展示信息 | 1.5d | **P0** | 首页就绪、Alert API 就绪、Elderly API 就绪 | 杨深铭（老人档案联调）、江松涛（告警列表联调） |
